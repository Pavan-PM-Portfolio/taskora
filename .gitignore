# config.js is committed on purpose: GitHub Pages serves it, and it only holds
# the publishable key. Never commit secret keys, passwords or dumps.
scripts/deploy.env
*.pem
.env
.env.*
backups/
dist/
supabase/.temp/
supabase/.branches/
node_modules/
.DS_Store
**/.DS_Store
